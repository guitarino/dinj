

describe('Circular dependency detection', () => {
    it('detects no circular dependency', () => {
    });

    describe('detects a circular dependency', () => {
        it('with warning enabled', () => {
            
        });

        it('with warning disabled', () => {

        });
    });

    describe('detects lazy circular dependency', () => {
        it('with warning by default', () => {
            
        });

        it('with warning enabled', () => {
            
        });

        it('with warning disabled', () => {

        });
    });

    describe('detects singleton circular dependency', () => {
        it('with warning by default', () => {
            
        });

        it('with warning enabled', () => {
            
        });

        it('with warning disabled', () => {

        });
    });

    it('detects both singleton and lazy dependency', () => {

    });
});